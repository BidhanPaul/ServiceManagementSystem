apiVersion: v1
kind: Service
metadata:
  name: servicemanagementsystem-service
spec:
  type: LoadBalancer
  selector:
    app: servicemanagementsystem
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080